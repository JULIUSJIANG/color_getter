namespace globalConfig {
    
    // 立方体边长
    export const FRAME_CUBE_SIDE_LENGTH = 1;

    // 用于拖拽的立方体边长
    export const DRAG_CUBE_SIDE_LENGTH = 0.5;

    // 画布尺寸
    export const CANVAS_SIZE = 400;
}

export default globalConfig;